# Path is resolved from android/app/build.gradle.kts, so this points to android/app/.keystore/upload-keystore.jks
storeFile=.keystore/upload-keystore.jks
storePassword=replace-me
keyAlias=upload
keyPassword=replace-me
